[project]
name = "saythe-spire2-installer"
version = "0.1.0"
description = "Installer for SayTheSpire2 accessibility mod"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "wxPython>=4.2",
    "requests>=2.31",
]

[dependency-groups]
dev = [
    "pyinstaller>=6.0",
]
